def CoinChange(coins, amount):
  int[] table = new int[amount + 1]
  set all values in table = amount + 1 (exceeds maximum number of coins possible if smallest coin is 1)
  table[0] = 0

  for sum in range (1, table.length):
    for coin in coins:
      if coin <= sum and table[sum - c] is not amount + 1
        table[sum] = min(table[sum], table[sum - c] + 1

  if table[amount] is amuont + 1:
    return -1
  else:
    return table[amount]
